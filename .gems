# To install these...
#  ...with Monk:   monk install
#   ...with RVM:   cat .gems | grep -v \# | xargs gem install
#  ...otherwise:   cat .gems | grep -v \# | xargs sudo gem install

# Runtime dependencies
sinatra -v "~> 1.1"
jsmin -v "~> 1.0"
haml -v ">= 3.0"
sinatra-content-for
maruku

# Test dependencies
ffaker
cutest -v "~> 1.0"
spawn
capybara
